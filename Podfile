# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

plugin 'cocoapods-binary'
use_frameworks!

target 'SecurePlace2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  pod 'Moya', :binary => true
  pod 'KeychainAccess', :binary => true
  pod 'MBProgressHUD', '~> 1.1.0', :binary => true
  pod 'LBTATools', :binary => true
  pod 'SnapKit', '~> 4.0.0', :binary => true
  pod 'Wendy', '~> 0.1.0-alpha', :binary => true
  pod 'DKImagePickerController', :binary => true
  pod 'SwipeSelectingCollectionView', :binary => true
  pod 'AssetsPickerViewController', '~> 2.0', :binary => true
  pod 'ImageViewer.swift', '~> 3.0', :binary => true
  
  # Pods for SecurePlace2

  target 'SecurePlace2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SecurePlace2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
